ORIGINAL TASK SPECIFICATION — VERBATIM EXTRACT (private)

Provenance and status
  Source: gonogo-evaluation-protocol/worker-prompt.txt, the actual task prompt
  given to the worker that produced PR #11. It predates the implementation.
  This extract was PREPARED AFTER THE MERGE from that pre-existing task prompt.
  It is NOT a new pre-implementation specification, and no acceptance criterion
  has been added, reworded, or tuned to the merged source or to any prior
  review outcome. Every line below is copied verbatim from the original.
  Omitted: operational budget, routing, command-scheduling and delivery
  instructions only. The omissions are itemised in extract-mapping.json.

Task gonogo-evaluation-packet-contract, tracking owieschon-1tnv.24. Implement one independently reviewable behavior: a researcher can validate a versioned, frozen evaluation packet before any reviewer sees it, with explicit provenance and disqualification reasons instead of silently counting exposed or mismatched material as untouched evaluation.

Work only in the assigned Superset workspace, branch feat/evaluation-packet-contract, base dd50207366d5d3033a8ae68a4788dfd840514911. You are not alone in the codebase. Preserve other branches and the canonical checkout. Read applicable AGENTS.md and the writing-style reference before editing docs. First inspect METHODS.md, existing evidence/subject identity and manual rating contracts. Reuse established hashing, validation and CLI conventions; add only the missing evaluation-packet seam. No new framework, dependency, event schema, database or judge backend.

Scope: one canonical protocol document (prefer extending METHODS.md rather than duplicating facts), a small offline validation command/helper with tests, and the minimum command documentation. Do not modify production judge prompts, rubric, thresholds, scores, blind-pass behavior, authentication, event history, replay recordings, or existing fixtures. Do not implement model panels, automatic merges, sampling infrastructure or metrics dashboards. No real holdout selection, source discovery, downloading production repos, or live judge/evaluation calls in this unit. All tests use clearly synthetic temporary data and are not study results.

Protocol: state the question as whether GoNoGo catches consequential errors missed by ordinary one-pass review under the same available evidence. Distinguish retrospective outcome-blinded evaluation from prospective agent-task calibration; original missing specs/transcripts must be marked missing, never reconstructed and presented as original. Predeclare selection/exclusion rules and a small human-review pilot with fixed stopping rules before future case access. Do not claim this unit has frozen a real study until a concrete manifest is reviewed and pinned. Keep participant exposure logs and known development cases outside any untouched holdout. Model pretraining contamination cannot be disproved by a manifest; state that limit. Do not equate merged PRs, passing tests, model-family agreement or one operator's labels with correctness. Separate human pre-model judgment from later adjudication informed by additional evidence; preserve uncertainty, disagreement, abstention and tool errors. Define false-go and false-alarm denominators, coverage, paired one-pass comparison, and human review time, without inventing results or precision unsupported by sample size. A pilot is feasibility evidence, not autonomous merge authorization.

Executable acceptance: use explicit versioned packet metadata, protocol/instrument digests and exact source/evidence identity. Validate actual payload bytes against declared digests, not merely nonempty strings. Fail closed on malformed/missing identity, changed frozen protocol or payload, different evidence for comparison arms, unknown provenance/exposure state, or forbidden post-cutoff/answer material in the declared review packet. Keep answer/outcome material separate from reviewer input. Exposed material can remain a labeled development case but cannot pass untouched-holdout eligibility. This is an integrity and eligibility check, not a detector of arbitrary semantic leakage in free text; do not claim otherwise. A valid synthetic packet must pass. Each refusal must name a stable reason. The command must be offline, read-only on caller inputs, make no judge calls, and leave original files intact on failure. Reuse the smallest incumbent types without treating existing subject_hash and rendered evidence_hash as interchangeable. Avoid mechanisms unrelated to these checks.

Before implementation, write failing contract tests and preserve exact first command/output privately; distinguish a missing-feature failure from a reproduced existing bug. Then prove those tests pass, run complete bun tests, existing replay gate, typecheck, diff check, and outgoing scope/secret/privacy scan. Do not fetch a missing formatter via bunx. Do not rerun unchanged failures to seek green. No broad installs/builds; available disk was about 10GiB. Check disk before tests, use existing runtime/dependencies where valid, and report exact blocker rather than copying node_modules or creating containers. No cleanup of unrelated resources.

Open an owned review PR only with accurate source/check receipts, no generated footer or private operational metadata. Do not merge or contact another repository owner. Repository rule6 requires a self-judge before merge: do not silently waive it. This unit forbids live judge calls while the evaluation contract is being prepared; report the pre-merge self-judge gate explicitly as pending, and do not represent a static cross-family implementation review as that self-judge. Do not change auth to bypass the known bare-mode issue. Preserve prior failures.

DONE means implementation finished for independent review, not validated scientific results or permission to merge.

