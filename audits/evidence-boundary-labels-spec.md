Correct operator-facing blind-input and confidence descriptions. The blind pass omits a separate spec attachment, but the opaque transcript can repeat the request. HTML and CLI confidence labels must say model-reported and uncalibrated. Align the README summary and add meaningful packet and rendered-output tests, retaining HTML escaping. Do not alter prompts, scoring, rubric, fixtures, or recorded evidence. Preserve unrelated history and quickstart sections. Pass typecheck, all tests, and replay evaluation.
