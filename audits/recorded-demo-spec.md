Add a credential-free demo command that composes existing fixture materialization, evidence collection, recorded replay, and HTML rendering. `bun run demo [output directory]` uses the committed merged-but-wrong sample1 and claude-sonnet-5 identity. It writes verdict.json and self-contained verdict.html with current version, subject hash, and replay provenance. It never calls a live judge, never falls back on a replay miss, and never changes the committed event log, fixtures, prompts, or replay receipts. Add focused tests for these boundaries and a quickstart. Correct the Bun prerequisite description. Preserve other behavior and pass typecheck, all tests, and the existing replay evaluation.
